---
import { humanize } from "@/lib/utils/textConverter";

const { className }: { className?: string } = Astro.props;

const paths = Astro.url.pathname.split("/").filter((x) => x);
let parts = [
  {
    label: "Home",
    href: "/",
    "aria-label": Astro.url.pathname === "/" ? "page" : undefined,
  },
];

paths.forEach((label: string, i: number) => {
  const href = `/${paths.slice(0, i + 1).join("/")}`;
  label !== "page" &&
    parts.push({
      label: humanize(label.replace(".html", "").replace(/[-_]/g, " ")) || "",
      href,
      "aria-label": Astro.url.pathname === href ? "page" : undefined,
    });
});
---

<nav aria-label="Breadcrumb" class={className}>
  <ol class="flex flex-wrap items-center gap-x-3 gap-y-2" role="list">
    {
      parts.map(({ label, ...attrs }, index) => (
        <li class="inline-flex items-center gap-3 capitalize" role="listitem">
          {index > 0 && <span class="text-text-light/70 dark:text-darkmode-text-light/70">&gt;</span>}
          {index !== parts.length - 1 ? (
            <a class="text-text-dark hover:text-primary dark:text-darkmode-text-dark dark:hover:text-darkmode-primary" {...attrs}>
              {label}
            </a>
          ) : (
            <span class="text-primary dark:text-darkmode-primary">
              {label}
            </span>
          )}
        </li>
      ))
    }
  </ol>
</nav>
